let secureURL = /^http/;   // match strings starting with http
let dotCom = /com$/;       // match strings ending with com
let bothStartAndEnd = /^radar$/; // having fun matching both ends of a string